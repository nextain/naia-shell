/**
 * 15 — skill_skill_manager AI path (#605)
 *
 * The gateway skill manager tool and SkillsTab install UI were removed.
 * Kept as an explicit skip so the inventory number stays stable.
 */
describe("15 — skill manager AI", () => {
	it("skill_skill_manager surface removed by #605", async () => {
		return; // intentionally empty — tool and UI gone
	});
});
