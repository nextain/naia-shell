# Dependencies
node_modules/
.pnp
.pnp.js

# Build artifacts
dist/
build/
*.tsbuildinfo
*.js.map

# Environment / secrets — NEVER commit
*.env
!*.env.example
*.json.local
.naia-agent.json
naia-agent.env

# Archive (local only)
*-old/
*-archive/

# OS
.DS_Store
Thumbs.db
ehthumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# AI 작업 임시물 + 주기 검증 러너 산출물 (로그/상태/PID — 추적 제외)
.agents/work/

# Editor
.vscode/
.idea/
*.swp
*.swo

# T3 보안 — 키/시크릿/개인정보 (절대 추적 금지, README 마커만)
data-private/
!data-private/README.md

# 보류 격리(Quarantine) — 방치 의심 자산 백업. 실물은 추적 제외, MANIFEST/README 만 추적
# (컨텍스트가 "백업 자산이 있었다"를 알도록). scripts/quarantine.mjs 가 관리.
quarantine/*
!quarantine/MANIFEST.json
!quarantine/README.md

# builds/ 생성물 (f0-graft-smoke.sh 가 만드는 붙여넣기 스니펫)
scripts/builds/f0-graft-snippet.js
scripts/builds/uc1-graft-snippet.js
packages/*/node_modules
packages/*/dist
packages/shell/src-tauri/target
packages/shell/src-tauri/gen
# 번들 바이너리 리소스(vosk dll, node.exe 등 ~140MB) — 빌드 시 복사, 추적 제외
packages/shell/src-tauri/resources/
# 배포 번들용 agent 스테이징(stage-agent.mjs 가 prod-hoisted deploy) — 빌드 산출물, 추적 제외
packages/shell/src-tauri/agent/
# 배포 번들용 BGM sidecar 스테이징(prod-hoisted deploy) — 빌드 산출물, 추적 제외
packages/shell/src-tauri/bgm-sidecar/
# 배포 번들용 cascade loader 스테이징(stage-cascade-loader.mjs 가 windows-manager loader 동봉) — 빌드 산출물, 추적 제외
packages/shell/src-tauri/cascade-loader/
# stage-runtime.mjs 가 platform-matrix.json 에서 생성하는 conf 오버레이 (#377 FR-INSTALL.2) — 생성물, 추적 제외
packages/shell/src-tauri/tauri.conf.generated.json

# 메인테이너 내부 운영물 — OSS 산출물 아님(개인 머신 경로/사이드카 레포·자격증명 의존).
# 데모 미디어 생성기 + 라이브 진단 하네스 + 수동 게이트웨이 스크립트는 공개 배포 제외.
packages/shell/e2e/demo-tts.ts
packages/shell/e2e/test-agent-tool.mjs
packages/shell/e2e/test-gateway-tools.mjs
packages/shell/e2e/uc-provider-provenance-live.spec.ts
packages/shell/e2e-tauri/specs/71-local-avatar-file.spec.ts
packages/shell/scripts/generate-lab-manual-images.mjs

# 로컬 게이트웨이 토큰 박힌 수동 헬퍼(CI 미참조) — OSS readiness
packages/shell/e2e/mini-node-host.mjs

# playwright 자동생성 산출물
packages/shell/test-results/
packages/shell/playwright.config.js
packages/shell/playwright.config.d.ts
packages/shell/vite.config.js
packages/shell/vite.config.d.ts
