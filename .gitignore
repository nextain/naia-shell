node_modules/
dist/
target/
.env
*.log
*.tsbuildinfo
.DS_Store
test-results/
playwright-report/
shell/e2e/demo-output/
shell/e2e-tauri/.artifacts/ui-message-trace.ndjson
shell/e2e-tauri/.artifacts/browser-console.ndjson
shell/e2e-tauri/.drivers/

# Flatpak build artifacts
flatpak-repo/
build-dir/
.flatpak-builder/
*.flatpak

# Claude Code (track settings + hooks, ignore the rest)
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/hooks/**
agent/.claude/

# Progress files (session-local, not committed)
.agents/progress/*.json

# Temporary work scripts (session-local, never committed)
.agents/work/
**/.agents/work/

# Windows build resources (downloaded at build time)
shell/src-tauri/resources/

# Temporary / local-only
errors/
iso-download/
work-logs/
.jikime/
*~
*.pid

# Cross-review target documents (transient inputs to reviewer agents).
# Per .agents/skills/cross-review/SKILL.md, only .jsonl event logs are
# committed artifacts — *-target.md files are working drafts.
.agents/reviews/*-target.md
.tmp-harness/
