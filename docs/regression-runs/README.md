# 회귀 실행 기록

기계마다 자기 몫의 실기 회귀를 돌리고 결과를 이 디렉터리에 남긴다.

```
node scripts/run-regression.mjs --machine=<이름> --tier=deterministic_ci[,credentialed_live,native_local]
```

남는 파일 하나가 그 기계의 한 번 실행이다. 담기는 것은 다음과 같다.

| 필드 | 뜻 |
|---|---|
| `planned` | 이 기계가 맡기로 한 스펙. **관측이 아니라 계획이다** |
| `executed` | 리포터가 판정을 읽은 스펙 — 통과든 실패든 **돈 것**이다. 완결성 판정은 이것만 센다 |
| `passedSpecs` | 그중 통과한 것. 채널의 `DONE` 한 줄이 쓰는 수 |
| `premise` | 그 세션들에 에이전트가 있었는가 — `ok` / `invalid` |
| `premiseSignals` | 그 판정의 근거 숫자(기동·리스 막힘·돈 스펙)와 이유 |
| `groups` | wdio 설정별 결과. 한 설정이 실패해도 다른 설정의 결과는 남는다 |
| `envMissingBeforeRun` | 요구 환경이 없어 **wdio 에 넘기지 않은** 스펙 |
| `harnessProvidedEnv` | 환경에는 없었지만 그 스펙의 wdio 설정이 스스로 채워 준 변수 |
| `envMissingGroups` | 스펙이 전부 환경 부재라 아예 띄우지 않은 wdio 설정 |
| `ranOn.inventorySha256` | 어느 스펙 목록을 잰 것인가 — 아래 지문 규칙 |
| `status` | `passed` / `failed` / `prerequisites-missing` |

**요구 환경이 없는 스펙은 실행하지 않고 기록에만 남는다.** 그러니 그 이름은
`executed`·`stableFailures`·`flakySpecs` 어디에도 오르지 않는다. 오래 반대로
돌았다 — 러너가 "환경이 없어 건너뛸 스펙" 이라고 찍어 놓고 그 목록을 그대로
wdio 에 넘겼고, 그중 하나는 `before all` 훅에서 키가 없다며 죽어 결함처럼
기록됐으며(재시도 한 번까지 썼다), 다른 하나는 스스로 통과해 재지 않은 채
`executed` 에 올랐다. 말한 것과 한 것이 다르면 기록은 두 갈래로 거짓이 된다.
실행에서 뺀 것이지 판정에서 뺀 것이 아니다 — 완결성 게이트는
`envMissingBeforeRun` 을 읽어 "이것은 통과가 아니다" 로 여전히 붉힌다. 선별은
`scripts/lib/regression-selection.mjs` 한 곳이 하고
`src/test/regression-selection.contract.test.ts` 가 고정한다. 무엇이 넘어가고
무엇이 빠지는지는 `--dry-run` 이 wdio 인자 그대로 보여 준다.

`planned` 와 `executed` 를 나눈 이유가 있다. 예전에는 계획을 그대로 배정으로
적었고 완결성 게이트가 그것을 덮인 것으로 셌다. 그러면 wdio 가 시작하자마자
죽어도 기록에는 전부 덮였다고 남는다. **손으로 기록을 만들 때 `executed` 를
빠뜨리면 그 기계의 몫은 전부 미실행으로 집계된다.**

### 돌리기 전에 알아야 할 것

**모든 스펙이 짝 naia-agent 체크아웃을 요구한다.** 전용 설정만 그렇다고 오래
믿었는데, 기본 설정(`e2e-tauri/wdio.conf.ts`)이 모듈 최상위에서 그것을 부르고
전용 설정들은 전부 그것을 상속한다. 짝이 없으면 스펙 전부가 설정을 읽는
단계에서 죽는다. 러너는 이 경우 아무것도 돌리지 않고 `prerequisites-missing`
으로 적는다 — 환경이 없는 것과 회귀가 깨진 것은 다른 사실이다.

짝은 **핀 커밋과 같고 작업 트리가 깨끗해야** 한다. 조금이라도 다르면 결과를
믿을 수 없기 때문이다. 설정이 기본으로 보는 자리는 저장소 옆의
`naia-agent-worktrees` 인데, 배치에 따라 다르므로 자리를 알려 주고 돌린다.

```
NAIA_AGENT_WORKTREES_DIR=<짝 워크트리들이 있는 디렉터리> \
NAIA_E2E_ARTIFACTS_DIR=<쓰기 가능한 디렉터리> \
  node scripts/run-regression.mjs --machine=<명단의 이름> --tier=deterministic_ci
```

### 어느 기계가 무엇을 맡는가

`machines.json` 이 명단이다. 기계 이름과 능력, 그리고 지금 참여하는지
(`active`)를 적는다. 러너는 그것을 읽어 **등급마다 그 등급을 맡은 기계들끼리**
스펙을 나눈다. 등급을 합쳐 나누면 균등하지 않다 — 다섯 대가 맡는 등급과 두
대가 맡는 등급을 한 줄로 세우면, 두 대짜리 등급의 몫이 다섯 대에 흩어져
그중 셋은 돌릴 수 없는 것을 받는다.

기계마다 `--peers` 를 손으로 적지 않는 이유도 같다. 목록이 어긋나면 몫이
겹치거나 비는데, 각 기계는 "내 몫을 다 돌았다" 고 말하므로 기록만 보고는 알
수 없다.

배포 전에는 이 기록을 모아 본다.

```
node scripts/check-regression-complete.mjs --max-age-hours=24
```

아무도 맡지 않은 스펙이 있거나, 환경이 없어 건너뛴 것이 있거나, 실패한
기계가 있으면 통과하지 않는다. **건너뛴 것은 통과가 아니다** — 그것이
리눅스 음성 결함이 넉 달 산 이유였다.

### 배포가 나가는 운영체제만 조건으로 삼는다

배포 매니페스트 `releases/v<셸 버전>.yaml` 에 `targets: [windows]` 처럼
그 배포가 나가는 운영체제를 적으면, 러너와 게이트는 **그 운영체제의 기계와
그 운영체제에서 도는 스펙만** 본다. 다른 운영체제 기계의 몫은 비어도
붉어지지 않고, 그 기계의 기록은 조건을 채우는 데 쓰이지 않는다 — 윈도우로
나가는 것은 윈도우에서 잰 것이어야 한다. `targets` 가 없으면 예전처럼 활성
기계 전부를 본다. 한 번만 다르게 보려면 `--platform=windows` 를 준다.

한 운영체제에서만 도는 스펙은 첫 줄에 `// platforms: linux` 를 적고, 다음
줄에 이유를 적는다. 인벤토리 생성기가 그것을 `platforms` 로 옮긴다. 구현은
`scripts/lib/release-target.mjs`.

### 이 기록이 믿을 만한 근거와 그 한계

기록에는 그 실행이 어느 코드·어느 스펙 목록에서 나왔는지가 함께 남는다
(`ranOn`: 인벤토리 해시, 커밋, 플랫폼, Node 판). 완결성 게이트는 그것을 보고
**다른 스펙 목록에서 돈 기록과 실행 시각이 미래인 기록을 판정에서 뺀다.**

**위조는 막지 못한다.** 기록은 저장소에 커밋되는 JSON 일 뿐이고, 커밋할 수
있는 사람이면 파일도 쓸 수 있다. 지문을 맞춰 적은 파일 하나로 "123개 전부
돌았다" 를 만들 수 있다 — 실제로 그렇게 되는 것을 확인했다. 코드로 구별할
방법은 없다.

여기서 막는 것은 **실수**다. 인벤토리가 바뀐 뒤의 낡은 기록, 다른 브랜치에서
돈 기록, 시계가 어긋난 기록. 이것들은 악의 없이도 생기고, 생기면 "전부
덮였다" 는 거짓 결론으로 이어진다. 신뢰의 경계는 저장소에 커밋할 수 있는
사람들이며, 그 경계 안에서 서로 정직하다는 전제 위에 이 프로세스가 선다.

기록은 커밋한다. 커밋하지 않으면 다른 기계가 그것을 볼 수 없고, 전체가
덮였는지 판단할 수 없다.

## 등급별 실제 수행 이력 (2026-09-05)

런칭 전 회귀가 실제로 무엇을 덮고 있었는지 기록으로 남긴다. 숫자를 좋게
보이려고 적는 것이 아니라, 다음 사람이 "다 돌렸다" 고 오해하지 않게 하려는
것이다.

| 등급 | 스펙 수 | 두 기계의 수행 이력 |
|---|---|---|
| `deterministic_ci` | 23 | naia-os-3090·win-rtx4060 이 나눠 여러 차례 수행 |
| `credentialed_live` | 90 | **2026-09-05 이전까지 두 기계 어디서도 한 번도 돌지 않음** |
| `native_local` | 8 | **두 기계 어디서도 한 번도 돌지 않음** |

즉 그날까지 런칭 전 회귀가 실제로 덮은 것은 121개 중 23개뿐이었다. 나머지
98개는 배정 목록에는 있었지만 아무도 수행하지 않았다.

돌지 않는 코드는 썩는다. 자격증명 등급을 처음 돌리자 그 등급에서만 불리는
`portIsOpen` 이 정의되지 않은 채 참조만 남아 있었고(러너가 ReferenceError 로
죽었다), 러너가 제품이 이미 없앤 `:18789` 게이트웨이를 전제로 요구해 45개를
통째로 막고 있었다. 둘 다 그 등급을 아무도 돌리지 않는 동안 생긴 것이다.

2026-09-05 naia-os-3090 첫 수행 결과는 44개 판정 중 6 통과, 38 실패였다.
대부분 한 뿌리다 — 모델에 닿지 못해(`provider error: fetch failed`) 그 뒤
단정이 줄줄이 무너진다. 이것은 제품 회귀가 아니라 준비 부족이며, 러너가
그 전제를 검사하지 않아 결함처럼 기록됐다.

## 자격증명 등급의 기본 공급자는 격리 ADK 에 심는다 (#547, 2026-09-06)

에이전트는 화면이 실어 보내는 provider 를 gRPC 경계에서 버리고, 워크스페이스의
`naia-settings/config.json` 으로 활성 공급자를 다시 세운다. 그래서 이 등급이
무엇을 부르는지는 **그 등급이 쓰는 워크스페이스**가 정한다. 이제 기본 wdio 설정이
`NAIA_API_KEY` 가 환경에 있을 때 실행 자리 아래에 워크스페이스를 하나 만들고
(`<실행 자리>/adk`), 거기에 나이아 게이트웨이 공급자를 심은 뒤 그 자리를 네이티브와
화면 양쪽의 정본으로 넘긴다. 키는 파일에 들어가지 않는다 — 설정에는 환경 변수
*이름*만 적히고, 값은 로그인과 같은 wire 로 에이전트에 실린다. 실행이 끝나면 그
워크스페이스는 실행 자리와 함께 지워진다.

**심는 것은 `llmRoles.main` 하나뿐이다 (#568).** 처음에는 최상위 `provider`/`model`
거울과 `onboardingComplete`, 이름·로케일 같은 화면 기본값까지 함께 적었다. 그러지
않으면 `ensureAppReady` 가 심은 것을 덮어쓴다고 보았는데, 코드를 읽고 실측해 보니
둘 다 틀렸다.

- `onboardingComplete` 는 **하이드레이션이 파일에서 지운다**(`mergeBootConfig` 이
  `delete normalizedFile.onboardingComplete` 를 하고 값은 로컬에서만 가져온다).
  파일에 적어도 `ensureAppReady` 를 건너뛰게 하지 못한다.
- `ensureAppReady` 는 덮어쓰기가 아니라 **병합**이다
  (`Object.assign(config, { provider: config.provider || "gemini", … })`).
  `llmRoles` 를 건드리지 않는다.

실제로 일어난 일은 그 반대였다. 최상위 거울이 하이드레이션을 타고 화면에 들어가면
`config.provider || "gemini"` 의 왼쪽이 참이 되어 **화면이 게이트웨이 공급자로
고정된다.** 그러면 공급자에 따라 갈리는 설정 목록이 달라져
`81-chat-tts-response` 가 `edge` 대신 `nextain` 을 받았다.

거울을 뺀 뒤 한 세션을 돌려 그 자리에서 잰 값이다.

| | 값 |
|---|---|
| 에이전트가 기동 시 고른 것 | `naia-settings(nextain/deepseek-v4-flash)` |
| 화면이 되쓴 `config.json` 의 `provider` | `gemini` (예전 기본값) |
| 그 파일의 `llmRoles.main` | `nextain/deepseek-v4-flash` (그대로 남는다) |
| `03-basic-chat` | EXIT=0 — 게이트웨이로 실제 대화 |

즉 화면은 예전처럼 `ensureAppReady` 가 세우고, 에이전트만 게이트웨이를 부른다.

따라서 이 등급의 결과는 사람이 쓰는 실제 ADK 의 설정과 무관하다. 그 ADK 의 활성
모델이 무엇이든, 어느 기계에서 돌리든 자격증명 등급은 같은 공급자를 쓴다. 반대로
키가 없으면 아무것도 심지 않고 예전처럼 돈다 — 러너의 전제 검사가 그 사실을
`NAIA_API_KEY` 부재로 먼저 말한다. 계약은
`src/test/credentialed-adk-seed.contract.test.ts` 가 고정한다.

이 시딩으로 `provider error: fetch failed` 는 사라졌다. 남은 실패는 다른 것들이다
— 도구를 부른 뒤 이어지는 답이 화면에 나타나지 않는 경우가 가장 많고, 그 다음이
스펙이 기대하는 도구(예: `skill_diagnostics`)가 에이전트에 없는 경우다. 벽 하나가
가리고 있던 것들이므로, 이제부터는 하나씩 따로 추적한다.

## 하네스가 채우는 변수

러너는 스펙이 요구하는 환경 변수를 실행 **전** 프로세스 환경과 대조해, 없으면
그 스펙을 wdio 에 넘기지 않는다. 그 판단이 옳으려면 "없다" 가 정말 없다는 뜻
이어야 하는데, 위의 시딩이 들어오면서 기본 설정이 `NAIA_E2E_ADK_PATH` 와
`NAIA_E2E_ADK_FIXTURE` 를 **자기 손으로** 격리 ADK 로 잡게 됐다. 두 변수는 밖에서
채우면 안 되는 값이기도 하다 — 실제 ADK 경로를 넣으면 화면은 실제 ADK, 네이티브는
격리 ADK 를 보는 분리가 다시 난다. 그래서 사람이 일부러 비워 둔 자리를 러너가
부재로 읽고, 이 기계의 자격증명 등급 마흔여섯 개 중 서른여덟 개를 빼 버렸다
(넘길 것 8 · 뺄 것 38). 시딩이 들어오기 전까지는 사람이 두 변수를 늘 손으로
넘겼기 때문에 드러나지 않았다. 지금은 선별이 그 둘을 부재로 세지 않고, `--dry-run`
이 `하네스가 채우는 변수 2개(시딩)` 한 줄로 몇 개가 왜 빠졌는지 밝힌다. 같은 환경
에서 뺄 것은 서른여덟에서 여섯으로 줄고, 남는 여섯은 진짜 부재다
(`NAIA_E2E_ADK_BASE`, `DISCORD_WEBHOOK`, `XAI_API_KEY`, `ELEVENLABS_API_KEY` 류).

목록은 러너에 적혀 있지 않다. 그 사실의 출처는 설정 자신이므로 정본을
`packages/shell/e2e-tauri/harness-provided-env.mjs` 한 곳에 두고, 시딩 모듈
(`credentialed-adk-seed.ts`)과 선별 모듈(`scripts/lib/regression-selection.mjs`)이
둘 다 거기서 읽는다 — 두 곳에 같은 목록을 적으면 다음에 하나가 바뀔 때 조용히
갈라지고, 갈라진 쪽은 십몇 분짜리 실행에서만 드러난다. 규칙이 붙는 설정은 기본
설정과 그것을 그대로 상속하는 `wdio.conf.chat.ts` 뿐이다. 전용 설정(codex,
radio-queue, voice-\*)은 자기 환경 모듈이 따로 있어 **다른 조건으로 다른 변수**를
채우므로 여기 묶지 않는다. 부재가 아니라고 해서 사라지지도 않는다 — 기록의
`harnessProvidedEnv` 칸에 무엇이 왜 부재가 아니었는지가 남는다. 계약은
`src/test/regression-selection.contract.test.ts` 가 고정하고, 그중 하나는 설정
소스를 파서로 읽어 정본이 말하는 변수를 설정이 **실제로 대입하는지** 본다.

## 스펙 목록 지문 — 줄끝에 흔들리지 않는다

기록에는 그 실행이 어느 스펙 목록을 잰 것인지가 `ranOn.inventorySha256` 으로
남고, 완결성 게이트는 그것이 지금 인벤토리와 같을 때만 기록을 판정에 넣는다.
인벤토리가 바뀐 뒤의 낡은 기록이 "전부 덮였다" 를 만들지 못하게 하려는
장치다. 그런데 이 지문을 `docs/e2e-inventory.json` 의 **원문 바이트** sha256 으로
잡았던 것이 문제였다. 윈도우 체크아웃은 같은 파일을 CRLF 로 받으므로 바이트가
다르고, 따라서 해시가 다르다. 실측하면 LF 판은 `a5e62ea6…`, 같은 내용의 CRLF
판은 `1420842c…` 이고 뒤쪽이 win-rtx4060 기록에 적힌 값이다. 두 기계가 같은
목록을 재고도 서로의 기록을 영원히 "다른 스펙 목록에서 돌았다" 로 버렸다.
여러 기계로 나눠 도는 것이 이 프로세스의 전부이므로, 이것은 나눔 자체를 무효로
만드는 결함이었다 — 4060 의 쉰아홉 개 실측이 통째로 판정 밖에 있었고 게이트는
그 몫을 "아무 기계도 맡지 않았다" 로 말했다.

이제 지문은 바이트가 아니라 **내용** 을 잰다. JSON 으로 읽어 키를 정렬해 다시
적은 다음 해시하므로 줄끝·들여쓰기·끝 개행·키 순서가 달라도 같은 목록이면 같은
지문이 나온다. 배열 순서는 그대로 둔다 — 스펙 순서가 기계별 몫을 가르는
입력이므로 순서가 바뀌면 실제로 다른 목록이다. 계산은
`scripts/lib/inventory-digest.mjs` 한 곳이 하고 러너와 게이트가 같은 함수를
부른다. 두 곳에 같은 식을 적으면 한쪽만 고쳐지고, 갈라진 것은 기록이 버려질
때에만 드러난다 — 이 결함이 정확히 그 모양이었다. 계약은
`src/test/inventory-digest.contract.test.ts` 가 고정한다.

**이행 기간은 끝났다(2026-09-07, #567 인벤토리 변경).** 아래 문단은 그 기간의 기록이다 — 규칙을 바꾸기 전에 두 기계가 이미 오늘의 실측을 push 했고
그 기록들은 옛 규칙으로 지문을 적었다. 새 규칙만 받으면 결함을 고치는 일이 이미
잰 것을 지우는 일이 되므로, 게이트는 당분간 옛 규칙(원문 바이트)의 LF·CRLF 판도
함께 인정하고 그렇게 받은 기록을 화면에 따로 밝힌다. **다음에 인벤토리가 바뀔
때 그 예외를 지운다** — 그때부터 옛 지문은 어차피 지금 목록을 가리키지 않고, 그
뒤의 기록은 전부 새 규칙으로 남는다. 계약 테스트의 마지막 단정이 그 시점에
붉어져 지울 자리를 알려 준다.

## 돈 것과 통과한 것은 다른 칸이다

`executed` 는 오래 두 뜻을 함께 담았다. 묶음이 실패하면 통과한 스펙만 담았고,
완결성 게이트는 그 칸을 "덮인 것" 으로 센다. 그래서 **실제로 돌아 실패한 스펙이
판정에서는 "아무 기계도 맡지 않았다" 로 보였다** — win-rtx4060 의 2026-09-05
기록은 쉰아홉 개를 맡아 열아홉만 `executed` 에 남겼고, 그 열아홉은 통과 수와
같았다. 재서 실패한 것을 재지 않은 것으로 말하면, 사람이 찾아야 할 결함이
빈칸으로 보인다.

지금은 `executed` 가 **리포터가 판정을 읽은** 스펙이다. 통과든 실패든 그 스펙은
돌았다. 한 줄도 읽히지 않은 스펙(wdio 가 시작하자마자 죽어 차례가 오지 않은
것)은 여전히 빠진다 — 그것은 정말로 돌지 않았다. 통과 수는 `passedSpecs` 가
따로 담고 채널의 `DONE` 한 줄이 그것을 쓴다. 실패가 덮인 것으로 세어져도 게이트가
초록이 되지는 않는다: 실패한 기계는 `status` 로 따로 붉어지고, 매번 실패하는
스펙은 `stableFailures` 에 남는다.

## 전제 — 그 세션들에 뇌가 있었는가

2026-09-06 이 저장소의 자격증명 등급 실행 하나가 마흔 개를 맡아 서른두 개를
안정 실패로 기록했다. 그런데 그 실패는 대부분 제품의 것이 아니었다 — 서른여덟
개 스펙 중 **서른일곱이 에이전트 없이 돌았다.** 앞 세션이 남긴 고아 agent 자식이
실행 자리의 리스를 쥐고 있어 다음 앱은 뇌를 띄우지 못한 채로 떴고, 앱은 멀쩡히
떠 있으니 스펙은 그대로 진행했다. 실패는 저마다 다른 자리에서 났다. 로그에는 그
사실이 그대로 있었다(`agent_lease_live_blocked` 37회, 성사된 기동 3회). **기록에는
한 글자도 없었다.** 그래서 사람이 그 숫자를 믿고 다른 기계의 실패와 가족별로
비교하는 헛일을 했다. 자세한 것은 `analysis-2026-09-06-credentialed.md` 에 있다.

로그는 다음 실행이 덮어쓴다. 그러니 세는 자리는 실행 자신이어야 한다. 이제 러너가
wdio 출력에서 두 가지를 센다 — 에이전트가 실제로 떴다고 네이티브가 밝힌 줄
(`agent-core started`)의 수와, 리스에 막힌 줄(`agent_lease_live_blocked`)의 수다.
기동 **시도**(`Starting agent-core: …`)나 재시작 잡음(`restart debounced`)은 세지
않는다. 막힌 세션일수록 그 잡음이 더 쏟아지므로, 그것을 세면 이 장치가 정확히
반대로 거짓말을 하게 된다.

판정은 단순하다. 스펙 하나가 세션 하나이고 세션 하나가 앱 기동 하나이므로, **돈
스펙 수만큼 에이전트가 떴어야** 한다. 기동 수가 `executed` 와 같고 막힌 세션이
없으면 `premise: "ok"`, 아니면 `"invalid"` 와 이유가 남는다. win-rtx4060 의 정상
실행이 그 성립한 모양이었다 — 기동 57회에 세션 57회, 막힘 0.

완결성 게이트는 `premise` 가 `"invalid"` 인 기록을 **덮인 것으로 세지 않고**
"전제 불성립 — 다시 돌려야 한다" 로 붉힌다. 그 실행의 실패는 제품의 것이 아니므로
결함으로도, 통과로도 세면 안 되기 때문이다. 이 칸이 아예 없는 옛 기록은 "미측정"
으로 따로 밝히되 덮인 것으로는 센다 — 알 수 없는 것을 아는 것처럼 세면 그 숫자가
다시 사람을 속이므로, 모른다는 사실 자체를 화면에 남긴다. 계약은
`src/test/run-premise.contract.test.ts` 가 그 실행의 로그 발췌로 고정한다.

전제가 성립해도 스펙은 실패할 수 있고, 그것이 진짜 신호다. 이 장치가 하는 일은
전제가 깨졌을 때 그 실행을 통과로도 결함으로도 세지 않게 하는 것뿐이다.
