{
  "name": "@nextain/naia-os-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "naia-os hexagonal transplant — F0 control-plane slice scaffold",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:report": "vitest run --reporter=verbose"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "exports": {
    "./shell-compat": "./dist/main/adapters/shell-compat.js",
    "./composition": "./dist/main/composition/index.js"
  }
}