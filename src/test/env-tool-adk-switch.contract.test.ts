// #582 UC-ENV-TOOL-RECOVER — ADK 전환 순서 (P02 골격, S3b 에서 채운다).
import { describe, it } from "vitest";

describe("#582 ADK 전환 (S3b 에서 구현)", () => {
  it.todo("A 의 감독자를 정상 종료한 뒤에만 B 의 lease 를 조정한다");
  it.todo("A 의 lease 파일이 B 아래로 새지 않는다");
});
