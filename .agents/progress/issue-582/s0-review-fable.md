# S0 리뷰 (Fable, 2026-09-09)

대상: ca9d3458(S0a), 5e8a02be(S0b), da7b2872(S0c). 방법: 세 커밋의 diff 정독, 서비스·도메인 코드와 테스트 대조, 계약(4.4·4.7·9절) 대조.

## 판정

계약대로다. 종결 CAS 는 양방향 경주를 테스트가 강제하고, 진행 중 멱등 공유는 동시 5건에 포트 호출 1회를 확인하며, deadline 은 가짜 타이머로 실제 만료를 밟는다. 취소 순서(CAS 선점 → 신호 → 포트)의 근거 설명이 맞다. 자원 RPC 를 증거 RPC 와 분리한 판단도 맞다.

## 고쳐야 할 것 (S0d)

1. **[P1] 터미널 exec 가 호출자 선언 등급을 그대로 믿는다.** `EnvironmentToolService.exec` 는 `admitEnvOperation(request…)` 에 요청의 `capability` 를 그대로 넘긴다. 관측 권한만 있는 조립에서 `capability: "observe"` 로 선언하면 명령이 통과한다(브라우저 RPC 만 표로 고정했다). 수정: 터미널 실행의 바닥 등급을 `workspace-write` 로 고정하고, 호출자가 더 높은 등급(destructive 등)을 선언하면 그 높은 쪽을 쓴다(낮추는 길은 막고 올리는 길은 둔다). 판정에 쓴 등급을 장부(`snapshotOf().tier`)에 남긴다. 테스트: 관측만 부여된 조립에서 `observe` 선언 exec 가 `capability-denied` 로 거부된다. 기존 터미널 테스트는 `workspace-write` 선언이라 영향 없다.

## 다음 슬라이스로 넘기는 것 (수정 아님, 기록)

2. **[P2] 자원 RPC 에 멱등 캐시가 없다.** `createWorkspace` 를 같은 멱등 키로 재전송하면(어댑터 재연결 뒤 흔한 일) 공간이 둘 생긴다. S3a 어댑터에서 감독자 쪽이 `idempotencyKey → workspaceId` 를 기억해 같은 공간을 돌려주도록 한다. FR-ENV-TOOL.9 의 "멱등 재전송" 을 자원 RPC 에도 적용하는 문장으로 요구사항을 보강한다.
3. **[P2] `cancel(unknownId)` 가 `cancelled` 로 답한다.** 모르는 작업의 취소는 "그런 작업 없음" 이 정직하다. S3a 에서 `Termination` 에 `unknown` 을 허용하거나 별도 결과로 바꾼다.
4. **[P2] 타임아웃 뒤 실제 중단은 어댑터 몫이다.** 서비스는 신호만 끊는다. S3a 계약 테스트 "deadline 만료 → 취소 부작용 정지" 가 이를 실제 Chromium 에서 확인해야 한다(이미 계획에 있음).

## 기준선 메모

S-1 문서 커밋이 UC-ENV-TOOL-SPACE 를 추가한 시점에 그 시나리오의 확인 수단이 없어 실패 1건이 생겼고(고아 시나리오 검사), S0a 가 첫 확인 수단을 붙여 사라졌다. 기준선 7건은 그대로다.

# S0d·S2a 리뷰 (Fable, 2026-09-09)

대상: c8d2adca(S0d), 1c69a09f(S2a). 재검증: `packages/ego-host npm test` 68건(63 pass, 5 todo, 0 fail) 종료 0, `--check` 0, `--provenance` 0 을 제가 직접 다시 돌렸다.

- S0d 는 리뷰 지적대로다. 모르는 등급 이름을 바닥으로 되돌린 판단이 맞다.
- S2a 핵심 불변 확인: 토큰은 감독자가 승인에 결박해 발급하고(`issueToken`), 핸드셰이크의 grant 가 발급 승인과 다르면 거부하며 연결의 grant 는 서버 기록을 쓴다. preload 는 벤더 모듈을 정적 import 하지 않고 `isMainThread` 가 아니면 아무것도 하지 않는다. 런처 stdio 는 `inherit` 세 칸 명시 목록, `shell:false`. sessionId 는 재작성하지 않고 중첩 `params.sessionId` 는 attach/detach 두 이벤트에서만 세션으로 읽는다.
- 계약과 달랐던 판단(useTaskSpace 관측 분류, 13초 상한, 짧은 deadline 우선, 미소유 이벤트 폐기·기록)은 전부 수용한다. 계획 4.2·4.4 에 반영할 문구: "공간 선택은 관측 등급", "감독자 상한 13초", "핸드셰이크 deadline 은 짧은 쪽".
- 남는 위험: 승인 없는 연결은 원시 CDP 를 전혀 보낼 수 없다(316행). 관측 도구(스냅샷·캡처)는 RPC 로 감독자가 대신 실행하므로 문제없지만, S2e 에서 스냅샷·캡처를 원시 CDP 로 구현하면 관측 연결이 막힌다. S2e 는 감독자 내부 CDP 연결로 실행해야 한다.

# S2b 리뷰 (Fable, 2026-09-09)

대상: b5d49257. 재검증: `npm test` 102건(98 pass, 4 todo, 0 fail) 종료 0, 테스트 뒤 marker 프로세스 잔류 0 을 직접 확인.

- 계약 4.8 의 추측이 실측으로 확정됐다: 감독자 SIGKILL 뒤 Chromium 은 파이프 EOF 로 약 200ms 안에 소멸(정리 코드 0줄). 후손 fd 탐침은 socketpair(`socket:[N]`)를 보도록 고쳐졌고, 일부러 넘긴 파이프를 잡아내는 것으로 탐침 자체를 검증했다.
- 계약과 달랐던 판단 넷(Chromium 이 cmdline 을 공백으로 재작성, libuv stdio 는 socketpair, Playwright 배치는 `chrome-linux64`, win32 조정은 회수하지 않고 `unverified`)은 전부 수용한다. 특히 win32 미확인 프로세스를 죽이지 않기로 한 것이 맞다. 계획 4.9 의 프로세스 확인 행을 이 사실로 고친다.
- 파이프 스트림을 종료 시 명시적으로 파괴하지 않으면 node 가 매달린다는 발견은 감독자 데몬 정상 종료의 조건이므로 4.8 에 적는다.
- 미실측 목록(win32 HANDLE 비상속·named pipe 기동·marker 확인, darwin 기동·회수)은 OS 별 게이트 항목으로 그대로 둔다.

# S2c·S2d 리뷰 (Fable, 2026-09-10)

대상: d84c619a, 3efe7391. 재검증: `npm test` 125건(123 pass, 2 todo, 0 fail) 종료 0, marker 잔류 0, 정책 파일에 접두사·와일드카드 없음, 소스 스캔 미분류 검사가 테스트 파일 최상위에 있어 하나라도 빠지면 파일 전체가 실패함을 확인.

- 계약과 달랐던 판단 셋(탭 생성은 라우팅 주인만·attach 만 배타, 감독자 자체 요청은 장부 소유를 만들지 않음, 대기 항목 소멸 뒤 늦은 attach 응답은 감독자가 detach)은 전부 수용한다. 첫째는 실행에서 실제로 막혔던 것이라 실측 근거가 있다. 계획 4.3.1 에 "배타는 attach 에 대한 것" 을 명시한다.
- 격리 행렬이 "A 에서 심긴 것을 먼저 확인" 하는 형태인 점이 맞다. 없음만 보면 심기 실패와 격리 성공을 구별 못 한다.
- S2e 로 넘긴 `operationHook`(requestId·objectId·GUID·IO 핸들 결속)과 `listTabs` 비대칭은 S2e 브리프에 명시한다.
- benchmark/.attest 변경은 루트 테스트가 갱신하는 타임스탬프이며 커밋 대상이 아니다. 머지 시 제외한다.

# S2e·S2f 리뷰 (Fable, 2026-09-10)

대상: 4329b695, c6d68d76. 재검증: `npm test` 146건 전부 통과·todo 0 종료 0, 헬퍼 행렬 `--check` 0, marker·cage 잔류 0.

- 무간섭 검증에서 계약 4.6 의 "Xvfb 필수" 대신 `cage` 헤드리스 컴포지터 위 Xwayland 로 잰 판단은 맞다. 사람 세션(Wayland `:0`)에서 재면 바뀔 수 없는 값을 확인하는 죽은 안전망이 된다는 실측이 근거이고, 계기가 살아 있음(창을 띄우면 활성 창이 실제로 바뀜)을 함께 증명했다. 계획 4.6 을 "Xvfb 또는 cage(wlroots 헤드리스)+xdotool" 로 고친다.
- 감독자가 자기 세션을 fail-closed 로 끊던 결함(S2c 시점부터 잠복)을 실브라우저 스냅샷이 처음 드러냈다. 스냅샷·캡처를 감독자 내부 CDP 로 돌리게 한 S2a 리뷰 지적이 이 결함을 조기에 노출시킨 셈이다. 유지.
- 업스트림 러너 미접속의 원인(`ego.helpers` 는 SDK import 경로에서만 생김)과 그것을 흉내내지 않은 판단은 맞다. 벤더 케이스·전문·픽스처를 그대로 import 해 우리 런처로 실행한 대안(지원 38·거부 4·범위 밖 2)을 S2f 의 결과로 인정한다.
- 헬퍼 행렬이 잡은 결함 셋(유령 탭, 낡은 주소로 새 탭, activateTarget 미반영)은 측정이 없었으면 S4 스킬에서 에이전트가 겪었을 문제다.
- CDP 거부 문구 끝에 안정 코드를 넣은 판단은 맞다(벤더가 `error.code` 를 버린다). 계획 4.3.2 에 적는다.

# S3a·S3b 리뷰 (Fable, 2026-09-10)

대상: f5d56660, a2b0c4ab. 재검증: 두 계약 테스트 파일 16건 통과 종료 0, marker 잔류 0, 어댑터에 `node:` import 없음, `script` 는 RPC 단위 승인 목록으로 승인 없이는 자식 프로세스 전에 거부.

- 감독자를 셸 프로세스 안에서 돌린 판단은 맞다. 파이프 부모 끝 유일 소유(4.8)와 취소 지연 최소화(4.7)가 근거이고, 코어의 `node:` import 금지 가드가 프로세스·파일 조작을 호스트 패키지에 가둔다.
- `.env` 를 SDK 저장소 루트에 두면 모든 ADK 가 공유해 남의 작업 id 와 토큰이 새는 문제를 실측으로 잡고 되돌린 것은 중요하다. 계획 4.2 의 "`.env` 두 곳" 을 "작업 공간 한 곳" 으로 고친다.
- `script` 등급을 `credential` 로 올리지 않고 RPC 단위 승인 목록을 둔 판단은 맞다. 등급을 올리면 자격증명 등급을 부여해야 해 비상속 원칙을 깬다. 계획 3절 4번·4.4 문구를 "터미널 바닥 등급 + 건별 승인" 으로 고친다.
- `backendNodeId` 재사용 발견은 안정 참조(FR-ENV-TOOL.3)의 실질적 위험이었다. 참조를 쓸 때 현재 스냅샷 refs 에 있는지 확인하는 규칙을 계획 4.4 에 적는다.

# S4·S6a·S6b 리뷰 (Fable, 2026-09-10)

대상: a0287afd, a897a643, 83607a74. 재검증: ego-host 155건 종료 0, Rust `cargo test ego_host` 14건 종료 0(짝 저장소 shell-pair-43ed7d0 환경), 시각 증거 11장 존재.

- S4 의 `cliLog` 판단("가르치는 자리에 0, 쓰지 말 것 절에만")은 맞다.
- S6b 가 e2e-tauri 실기로 잡은 결함(sysinfo 기본 갱신에 `cmd` 없음 → 회수 0회)은 주입 단위 테스트가 못 잡는 종류다. 실기 검증을 건너뛰지 않은 결과다. `browser.deleteSession()` 이 정상 종료가 아니라는 실측도 유지.
- **미완(S6c 로 닫는다)**: 셸 웹뷰에는 Node 가 없어 도구 호출이 실제 감독자에 닿는 경로가 없다. S6a 의 Playwright 는 어댑터 아래를 대역으로 바꿨고, 프로덕션에서는 코어 기본 로더 실패가 형식 있는 거부로 올라간다. 테스트가 초록이어도 사용자는 쓸 수 없으므로 "완료" 가 아니다. 다리는 Rust 가 Node 감독자를 소유 자식으로 띄우고(BGM 사이드카·`find_node_binary` 선례) 소켓으로 말하며, 웹뷰는 Tauri 명령으로만 부르는 형태로 둔다. 완료 기준은 실제 Tauri 앱에서 도구 호출 → 실 Chromium 증거 반환 e2e-tauri.
- 기존 `e2e/env-tool-browser.spec.ts` 첫 케이스는 변경 전에도 실패한다(대역이 `read_naia_config` 에 문자열 아닌 값). 다른 세션 파일이라 손대지 않은 판단 유지. 머지 뒤 별도 이슈로.

# S6c 리뷰와 S5 검토 (Fable, 2026-09-10)

대상: af909a91, 669f874c, bf831dbe, 52c1e060. 재검증: ego-host 164건 종료 0, 셸 vitest 신규 34건 종료 0, marker·supervisord 잔류 0(pgrep 자기 매칭 1건 제외), 관리 비밀은 spawn env 로만 전달되고 파일·로그 기록 없음, 공유 파일 변경은 lib.rs 19줄·App.tsx 10줄·ChatArea.tsx 45줄(함수 정의 없음).

- 포트 셋을 웹뷰에서 다시 쓰지 않고 `EgoHostApi` 한 겹만 IPC 로 바꾼 판단은 맞다. 안정 참조 검사·개정 대조·증거 조립이 코어 어댑터 한 곳에 남아 S3a 계약 테스트가 웹뷰 동작을 계속 증명한다.
- 심링크 패키징 사고(리소스 복사가 원본을 덮어 0바이트)는 실기가 잡았고, 방어가 빌드 스크립트에 들어갔다.
- 풀스택 e2e 는 뇌를 거치지 않고 셸의 도구 분기를 직접 트리거한다. 뇌→셸 경로는 기존 `app_tool_call` 배선이며 이번 변경이 그 경로를 바꾸지 않았다.

S5 검토 결과.
- UC 세 편을 구현과 문장 단위로 대조했다. 검증되지 않은 문장 하나(UC-SCRIPT 의 "승인된 묶음 실행도 경계를 벗어나지 않는다")는 FR-ENV-TOOL.14b Pending 으로 분리해 적었다. 승인 UI 와 샌드박스 경계 강제는 후속 이슈다.
- FR 상태: .2a·.6·.9·.10~.15·NFR-VENDOR·NFR-ABI 를 Done 으로, 각각 근거를 검증 열에 병기. NFR-OS.1 은 In-progress(리눅스 통과, win32·darwin 미실측, 플래그 기본 끔). .2b·.14b Pending.
- 커버리지 맵: S6c 산출물 세 행 추가. 가리키는 파일 전부 존재, todo 0. 추적 검사 3종·e2e 인벤토리 0.
- 레지스트리: UC-025 Approved, TEST-S-026·TEST-F-018 Pass(linux), SPEC-018 Done(linux).
- QC 조각 12건(풀스택 케이스 추가). 번호는 Linux 2회차 종료 후 QC-206 부터.
- process-status.json 에 current_work 항목을 텍스트 최소 삽입(17줄)으로 추가. 헌장 파일이지만 관문 절차가 요구하는 산출물이고 루크가 이 작업을 지시했다.
