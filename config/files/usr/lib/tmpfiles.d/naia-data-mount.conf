# Persistent mount target for naia-data partition (issue #262).
# /var is regenerated per-deployment on rpm-ostree systems, so directories
# under /var must be declared via tmpfiles.d to survive boot.
d /var/naia 0755 root root -
